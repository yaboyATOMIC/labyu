const textElement = document.getElementById('text-portrait');
const message = "I LOVE YOU ";
// Repeat the message enough times to cover the portrait area (e.g., 5000 times)
const repeatedMessage = message.repeat(5000); 
textElement.textContent = repeatedMessage;
